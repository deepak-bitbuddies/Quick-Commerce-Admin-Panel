import { DEFAULT_PAGE_SIZE } from "@/constants"

/** Matches `.claude/ui/playbooks/filter-bar.md`'s recommended search debounce. */
export const BRAND_SEARCH_DEBOUNCE_MS = 300

/** Matches the backend's default `pageSize` for the brands list endpoint. */
export const DEFAULT_BRAND_PAGE_SIZE = DEFAULT_PAGE_SIZE
